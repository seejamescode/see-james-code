---
applications:
- name: seejamescode
  domains:
    - seejamescode.com
  hosts:
    - seejamescode
    - www
  no-hostname: true
  command: npm run prod
  instances: 1
  memory: 512MB
  services:
   - seejamescode_env
  